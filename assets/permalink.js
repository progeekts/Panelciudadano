/* Enlaces compartibles sin backend: #ficha=tipo:id */
(()=>{
const PREFIX='#ficha=';
function encodedKey(){const h=location.hash||'';if(!h.startsWith(PREFIX))return'';try{return decodeURIComponent(h.slice(PREFIX.length))}catch{return''}}
function openFromUrl(){const key=encodedKey(),api=window.PanelCiudadano;if(!key||!api?.getItemByKey)return;const x=api.getItemByKey(key);if(x)openCard(x.type,x.id)}
function enhance(){const modal=document.querySelector('#modal'),content=document.querySelector('#modalContent'),api=window.PanelCiudadano;if(!modal||modal.hidden||!content||!api)return;const key=modal.dataset.itemKey;if(!key||content.querySelector('.share-row'))return;const x=api.getItemByKey?.(key);if(!x)return;const row=document.createElement('div');row.className='share-row';row.innerHTML='<button type="button" data-copy-link>Copiar enlace a esta ficha</button><small>Enlace permanente dentro de Panel Ciudadano</small>';content.append(row);row.querySelector('[data-copy-link]').onclick=async()=>{const u=new URL(location.href);u.hash=`ficha=${encodeURIComponent(key)}`;try{await navigator.clipboard.writeText(u.href);row.querySelector('button').textContent='Enlace copiado ✓'}catch{history.replaceState(null,'',u);row.querySelector('button').textContent='Enlace preparado en la barra del navegador'}}}
const baseOpen=openCard;openCard=function(type,id){baseOpen(type,id);const key=document.querySelector('#modal')?.dataset.itemKey;if(key&&id!==undefined)history.replaceState(null,'',`${location.pathname}${location.search}${PREFIX}${encodeURIComponent(key)}`);enhance()};
document.addEventListener('panel:ready',openFromUrl);window.addEventListener('hashchange',openFromUrl);document.addEventListener('click',e=>{if(e.target.matches('[data-close],.backdrop')&&location.hash.startsWith(PREFIX))history.replaceState(null,'',location.pathname+location.search)});window.addEventListener('DOMContentLoaded',()=>{if(window.PanelReadiness?.complete)openFromUrl()});
})();